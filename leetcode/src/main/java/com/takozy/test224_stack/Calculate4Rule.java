package com.takozy.test224_stack;

import java.util.ArrayList;

/**
 * 四则运算
 *
 */
public class Calculate4Rule {

    /**
     * 转换中缀表达式 -> 后缀表达式
     * @param s
     * @return
     */
    public static ArrayList<Object> changePosture(String s) {
        ArrayList<Object> list = new ArrayList<>();
        /*char[] chars = s.toCharArray();
        String num = '';
        for (char i : chars) {
            if (i <= '0' && i >= '9') {
                num =
            }
        }
*/
        return list;
    }
}
